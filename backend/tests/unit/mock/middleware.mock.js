const mockErroMensNameRequire = {
  message: '"name" is required',
};

const mockErroMensNameMoreFiveChar = {
  message: '"name" length must be at least 5 characters long',
};

module.exports = {
  mockErroMensNameRequire,
  mockErroMensNameMoreFiveChar,
};